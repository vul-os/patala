/target
Cargo.lock
**/*.rs.bk
.claude/
__pycache__/
*.pyc
.venv/
dist/
*.so
*.dylib
# UniFFI-generated Python bindings + the cdylib copied next to them are build
# output, reproduced by `patala-py/README.md`'s documented `uniffi-bindgen`
# command — not checked in (same reasoning as `/target`).
/patala-py/bindings/
