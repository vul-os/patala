# kotlinc / javac output
/out/
/build/
/target/

# Generated UniFFI Kotlin, when uniffi-kotlin-probe.sh is run by hand outside
# its own mktemp dir. Never committed: it is generated output, and the probe
# regenerates it every run.
/bindings/
