# kotlinc / javac output
/out/
/build/
/target/

# The generated UniFFI Kotlin this SDK is built from, plus anything the probe
# leaves behind. Never committed: it is build output, reproduced exactly by
# `make -C sdks/kotlin generate` — same reasoning as /target,
# /patala-py/bindings and /patala-go/bindings.
/bindings/
